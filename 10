Program to add two numbers using Function with no arguments and no return value
def add_numbers():
    num1 = float(input("Enter first number: "))
    num2 = float(input("Enter second number: "))
    sum = num1 + num2
    print("The sum of the numbers is:", sum)

# Call the function
add_numbers()
